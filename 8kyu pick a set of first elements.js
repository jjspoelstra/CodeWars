function first(arr, n=1) {
    return arr.slice(0, n)
  }
  
  
  //parameters: given an array and a number of elements in the array to isolate.
  //return the isolated elements as a new array
  //example shown
  //pseudo:
    //slice the array, starting at index 0, goinmg until the number
    //return a new array from the sliced one
    //n should default to one